{
  "name": "school-system-cloudflare",
  "version": "1.0.0",
  "description": "School System on Cloudflare Workers",
  "main": "api/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
